import BusRoutes from '@/components/BusRoutes'

export const metadata = {
    title: 'Bus Routes | Cloud Community Club',
    description: 'Search and view SNIST bus routes, boarding points, and pickup timings.',
}

export default function BusRoutesPage() {
    return <BusRoutes />
}
